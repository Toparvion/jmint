package ru.ftc.upc.testing.dropper.lang.samples;

import java.util.Currency;
import Object;                  // must be ignored by assembler
import sun.misc.BASE64Encoder;
import java.math.*;

import static java.lang.Double.*;
import static java.lang.Integer.MAX_VALUE;
import static java.lang.Math.random;

class Imports {

  public Imports() {
    System.out.println();
    BigInteger bigInteger = new BigInteger("31415926525");
    double random = random();
    int maxInt = MAX_VALUE;
    System.out.printf("%d, %d, %d", MAX_EXPONENT, MIN_EXPONENT, MIN_VALUE);
  }
}